import { Request } from 'express';

export async function storeDocument(req: Request) {
    try {
        


    } catch (error) {
        console.error(error);
        return { ok: false

        };
    }

    return { ok: true };
    
}